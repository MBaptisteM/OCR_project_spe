#include "training.h"
#include "extract_data.h"



int main(int argc, char *argv[]){
    /*
    if (argc < 2)
        errx(EXIT_FAILURE, "too few arguments for AI");
    if (atoi(argv[1]) == 0){
        while (training(1792) != 100);
    }
    else{
        if (argc < 3)
            errx(EXIT_FAILURE, "too few arguments for AI, need to add the path");
        
        load("weights.txt");
        printf("character for %s is : %c\n", argv[2], get_character(argv[2]));
    }
        */
    return EXIT_SUCCESS;
}